<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.laboratory1">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/airplane_ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/airplane_ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Laboratory1">
        <activity android:name=".ThirdActivity"></activity>
        <activity android:name=".game.GameActivity" />
        <activity android:name=".SecondActivity" />
        <activity android:name=".SixthActivity" />
        <activity
            android:name=".FourthActivity"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".FifthActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>